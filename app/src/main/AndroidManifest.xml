<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a1114049_1090780_iiatimd_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.1114049_1090780_iiatimd_app.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".recipeInstructionsDetail"></activity>
        <activity android:name=".recipeIngredientsDetail" />
        <activity android:name=".recipeDetailActivity" />
        <activity android:name=".newRecipeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>